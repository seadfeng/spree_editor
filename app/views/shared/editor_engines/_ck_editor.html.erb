<%= javascript_include_tag Ckeditor.cdn_url %> 

  <script>
    $(function() {
      <% ids.each do |id| %>
        if ($("#<%= id %>").length > 0) {
          CKEDITOR.replace("<%= id %>", {"customConfig": "<%= javascript_url 'ckeditor/config.js' %>" });
        }
      <% end %>
    });
</script>